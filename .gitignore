# Build artifacts
# P-001 target: builds/macos|windows|linux — keep Builds/ until migration
build/
builds/
Builds/
*.o
*.obj

# IDE
.vscode/.cmaketools.json
.vscode/c_cpp_properties.json
.DS_Store

# Cache directories
.cache/

# Xcode
*.xcodeproj
*.xcworkspace
DerivedData/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# Binaries
*.app
*.component
*.vst3
*.exe
*.dll
*.dylib
*.so

# Tools
Tools/

# Runtime logs (P-001: logs/ target; keep Logs/ during transition — D-094)
logs/
Logs/

# Legacy source archives (not for release)
src/Archives/

# Personal workspace (never version: docs, mockups, TestLab, profiling, etc.)
_local/

# Legacy root folders — ignore if recreated before P-001 migration
Documentation/
Quality/
Workbench/
