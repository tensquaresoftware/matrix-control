# Matrix-Control — Project Instructions

Personal coding, communication and Git conventions live in `.cursorrules`
(single source of truth, shared with Cursor). They are imported below so they
apply to Claude Code as well.

@.cursorrules

## Claude Code specifics

- **Language**: reply to Guillaume in French, informal "tu" (see imported rules §1).
- **Commits**: English only, imperative summary line + bullet list of significant
  changes; create commits only on explicit request (see imported rules §6.2).
- **C++ naming & style**: follow the imported conventions (§6 and §7) — naming,
  Allman braces, 4-space indent, `k` prefix for constants/enum values, private
  member underscore suffix, etc.
- **BMad Method** drives the workflow (skills under `.claude/skills/`); workflow
  state lives in `_bmad-output/implementation-artifacts/sprint-status.yaml`.
