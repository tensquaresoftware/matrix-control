# Chore: JUCE project generator v2 structure

**Status:** done  
**Date:** 2026-05-30  
**Branch:** `chore/juce-project-generator-v2-structure`

## Summary

Reverted Epic E0 lowercase root renames and aligned the brownfield repo with the desktop reference project generated by JUCE project generator v2.

## Changes

- `src/` → `Source/`, `assets/` → `Assets/`, `tests/` → `Tests/`, `docs/` → `Documentation/`
- Removed `configure-platform.py` (legacy generator)
- Added `project-configuration.cmake`, updated `CMakeUserPresets.json`, merged `CMakeLists.txt` (presets, JUCE_DIR discovery, artefacts copy)
- IDE: reference `.vscode` (CMake Tools, `clangd` disabled), removed `.clangd`
- Kept `ProjectPaths` and runtime `logs/` layout
- Epic E0 stories 0.1, 0.2, 0.4 marked cancelled in sprint-status; 0.3 retained (ProjectPaths)

## Verification

- Configure: `cmake --preset default-macos-arm64`
- Build: `cmake --build Builds/macOS/ARM --target Matrix-Control_Standalone`

## Follow-up fixes (2026-05-30)

- `Logs/MIDI/`, `Logs/APVTS/` (PascalCase) via `ProjectPaths`
- `Tests/Unit/` no longer linked into plugin targets (avoids `Builds/.../Tests/` and host crashes)
- Daily builds: prefer `default-macos-arm64`, not Universal, unless needed
- IDE/CMake: aligned with generator template (no custom `cmake.buildTarget`, no `Matrix-Control_All` → `CopyToArtefactsDir` dependency). Deploy plugins/artefacts via **Build: All** (default `all` target), not Standalone-only.
