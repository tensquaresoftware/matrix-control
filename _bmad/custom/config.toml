# Team / enterprise overrides for _bmad/config.toml.
# Committed to the repo — applies to every developer on the project.
# Tables deep-merge over base config; keyed entries merge by key.
#
# Language policy (Guillaume / Matrix-Control):
#   - Chat with the user (communication_language): French
#   - Generated BMad artifacts (document_output_language): English
# Personal overrides: _bmad/custom/config.user.toml (gitignored)

[core]
communication_language = "French"
document_output_language = "English"
